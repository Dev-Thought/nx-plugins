{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "NxDeployIt Deploy infrastructure",
  "description": "Deploy the infrastructure for the app",
  "type": "object",
  "properties": {
    "nonInteractive": {
      "type": "boolean",
      "default": false,
      "description": "Changes will apply without user input"
    }
  },
  "required": []
}
