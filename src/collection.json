{
  "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "predictive-setup": {
      "description": "Search and asks you which applications can be setup by ng-deploy-it",
      "factory": "./predictive-setup/index",
      "schema": "./predictive-setup/schema.json",
      "hidden": true,
      "aliases": ["ng-add"]
    },
    "init": {
      "description": "Initialize infrastructure as code for an application",
      "factory": "./init/index",
      "schema": "./init/schema.json"
    }
  }
}
